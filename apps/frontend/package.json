{
  "name": "@flash-sale/frontend",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "echo 'Frontend dev server - configured in PR12'",
    "lint": "eslint \"src/**/*.{ts,tsx}\"",
    "lint:fix": "eslint \"src/**/*.{ts,tsx}\" --fix",
    "test": "echo 'Tests configured in later PRs'"
  },
  "dependencies": {
    "@flash-sale/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
